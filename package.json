{
  "name": "courses-catalog",
  "private": true,
  "scripts": {
    "sass": "sass scss/main.scss css/main.css --no-source-map",
    "sass:watch": "sass --watch scss/main.scss:css/main.css --no-source-map"
  },
  "devDependencies": {
    "sass": "^1.89.2"
  }
}
